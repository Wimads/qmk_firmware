COMB(DEF_FWD,   FWD,        MMB, RMB)      // _ X X
COMB(DEF_BCK,   BCK,        LMB, MMB)      // X X _
COMB(DEF_REF,   KC_F5,      LMB, RMB)      // X _ X
COMB(DEF_NO,    KC_NO,      LMB, MMB, RMB) // X X X (still open combo slot)
